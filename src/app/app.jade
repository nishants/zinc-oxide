#app.state(ng-app='zinc', ng-class="state.name")
  #top-bar
    include navigation/top-bar/top-bar
  #content
    div(ui-view)
  #state-loader(ng-class="{'loading' : state.loading}")
  ul#flash-messages
    include components/flash-messages/flash-messages